<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yucel.projetakipuygulamasi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".menuActivity" />
        <activity android:name=".yeniProjeActivity" />
        <activity android:name=".tumProjelerActivity" />
        <activity android:name=".devamEdenProjelerActivity" />
        <activity android:name=".testEdilmisProjelerActivity" />
        <activity android:name=".arsivActivity" />
        <activity android:name=".tamamlanmisProjelerActivity" />
        <activity android:name=".yapilacaklarActivity" />
        <activity android:name=".projeEkibiActivity" />
        <activity android:name=".personelEkleActivity" />
        <activity android:name=".personelBilgileriActivity" />
        <activity android:name=".gorevlerActivity"></activity>
    </application>

</manifest>